我在akile买了个1核1g的香港服务器,有独立ipv4和独立ipv6,我觉得建站很合适,今天我手写了一个动态博客,本来使用nodejs做后端,mysql做数据库,nginx做反向代理.结果发现东西写完之后在机器跑不起来.运行mysql后运行nodejs连第一句console.log('The server is running on port 3000')都运行不起来,换成sqlite3做数据库,就可以运行了,毕竟这数据库只是个文件,和庞大的mysql相比轻量很多.有个麻烦的事是sqlite3在npm不能安装,好像要自己编译,然后我懒得编译,就用yarn下载,我一般是不用yarn的,因为不熟悉,但也只能这么干了,在vps上我运行npm install,就直接安装了sqlite3,我不知道是通过npm还是yarn安装的,但是部署的时候又有问题了.我ip加端口访问我的nodejs完全没问题,但是域名就是打不开,nginx和nodejs都没有报错,我只能放弃nginx,直接用nodejs开放443端口.